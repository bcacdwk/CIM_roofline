# 十例本轮统一约定

当前审阅入口为 `TEN_CASE_REVIEW.zh.md`，既有两份审阅保留为历史依据。

- 身份使用“介质＋状态模式＋计算路径＋关键资源”；R0是起点，资源/组织变更明确记录，非材料必需项。
- B_S/B_R只计有效逻辑Byte；共同B_S=128 Byte、矩阵16384 Byte。每例说明更新形状、完整步骤与矩阵聚合；内部编码、读验、恢复不增加payload。
- 推荐参考点、短/参考/长成对条件工程情景、固定其他因素的独立敏感性、资源对照、临界/不可行情景分列。三点极值不是物理不确定性包络。
- 原始服务占用含事务必需恢复，周期刷新另给占用、可用率和长期有效间隔。α≤0为不可行，吞吐/ridge置空，不裁正；共同α抵消不代表能力不受损。
- 摘要统一十进制MB/s（10^6 Byte/s），机器时间ns，未取整值保留。整矩阵为16 KiB，非16 kB。完整周期按覆盖关系计费，不重复追加阶段。
- 证据分实测/仿真原量、工程选择、推导；RRAM HV、PCM驱动、FeFET观察窗均按工程预留标注。终态验收保留，预算内达标为条件而非成功概率证明。
- 每例保留独立章节/PDF和来源入口；新增紧凑结果卡。TeX label及引用加案例前缀；字体沿用ctex/Fandol。统一导出由主Agent适配各例结果，不改共享理论/参数。
- 子Agent仅修改获分配的案例目录；主Agent负责公共文件、十例结果卡与JSON/CSV适配、独立复算和最终PDF复核。不得改literature/、主论文、Table II，不执行Git写操作。
